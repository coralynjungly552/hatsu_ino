# hatsu_ino configuration
# Copy this file to the root of your SD card.
# Lines starting with # are comments and are ignored.

# Volume: 0 (silent) to 7 (max). Default: 6
VOLUME=6

# Playback mode:
#   RANDOM     - picks a random WAV file each ignition cycle,
#                never repeating the same track twice in a row (default)
#   SEQUENTIAL - plays WAV files in order, advancing one track per cycle
#   SINGLE     - always plays the file set by TRACK= below
MODE=RANDOM

# Track to play in SINGLE mode (ignored in RANDOM and SEQUENTIAL modes).
# Must be a valid 8.3 WAV filename matching a file on the SD card.
# TRACK=STARTUP.WAV
